export default function BasketList (): JSX.Element {
const basketCamerasData;
  return (
    basketCamerasData.map((basketCamera) => {
        <BasketListItem basketListItem={basketCamera}/>
    })
  );
}
